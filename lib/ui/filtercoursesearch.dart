import 'package:flutter/material.dart';

class FilterCourseSearch extends StatelessWidget {
  const FilterCourseSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Row(
        children: [
          ListView(
            children: [],
          )
        ],
      ),
    );
  }
}
